BrayCC Spin Hub — Changelog
============================

Version: ICE Contacts + Admin Panel
Date: 2026-09-25


FILES CREATED
-------------
netlify/functions/ice.js
  - New endpoint GET /api/ice?id=<spinId>&user=<name>
  - Returns ICE contacts for a spin, only to the spin proposer.
  - Returns 403 if requesting user is not the proposer.

netlify/functions/cleanup-ice.js
  - Scheduled function. Runs daily at 03:00 UTC.
  - Removes ICE details from any committed rider whose spin start
    time was more than 24 hours ago.

netlify/functions/admin.js
  - New admin API. Protected by X-Admin-Token header matching
    ADMIN_TOKEN_SECRET env var.
  - GET    /api/admin?full=1        → returns all spins incl. ICE + subs
  - PATCH  /api/admin?id=<spinId>   → edit any spin field
  - DELETE /api/admin?id=<spinId>   → delete entire spin
  - DELETE /api/admin?id=<spinId>&rider=<name>
                                    → remove one rider from a spin

public/admin.html
  - Full admin UI at /admin.html
  - Password-protected login (uses ADMIN_TOKEN_SECRET).
  - Lists all spins with full ICE details visible.
  - Edit any spin: title, type, date, time, location, distance,
    pace, minRiders, weather, author, phone, mapLink, mudguards.
  - Edit ICE per rider inside a spin.
  - Delete any spin, remove any committed rider, remove interested.
  - Shows push subscriber count and a list of subscriptions.

public/admin.js
  - Client logic for the admin panel.
  - Caches admin token in sessionStorage so it survives page refresh
    but not browser close.

ChangesUpdatesChangeLog.txt
  - This file.


FILES MODIFIED
--------------
netlify/functions/spins.js
  - Committed riders are now objects { name, ice, committedAt }
    instead of plain strings.
  - PATCH now REQUIRES an "ice" object when action is "committed":
    { name, phone, relation?, notes? }
  - GET returns sanitised spins: ICE values stripped, but a
    "hasICE" boolean is included per committed rider.
  - Read-time purge: any spin older than 24 hours has ICE wiped on
    every GET, as a fallback to the scheduled cleanup.
  - Backwards-compatible: existing spins with committed as
    ["name1","name2"] still work.

public/app.js
  - Committing now opens an ICE modal. Without ICE details the
    commit is rejected by the server.
  - Proposer sees a red "ICE Contacts" button on their own spins
    (only if at least one rider has ICE on file).
  - That button opens a modal that fetches /api/ice and displays
    each rider's ICE contact.
  - Handles the new committed-as-objects data shape.

public/index.html
  - Added two modals before </main>:
      #ice-modal       → ICE entry when committing
      #ice-view-modal  → ICE list shown to the proposer
  - Preserved all pace group updates from the previous release
    (Red=Leisurely, Orange=Moderate, Yellow=Steady, Green=Brisk,
    Blue=Fastest).

public/sw.js
  - Bumped cache version braycc-v1 → braycc-v3 so browsers
    fetch the new app.js and index.html.


ENVIRONMENT VARIABLES
---------------------
No new variables required. Uses existing:
  - ADMIN_TOKEN_SECRET   (for admin.js auth)


URL ROUTES ADDED
----------------
GET    /admin.html
GET    /api/ice?id=<spinId>&user=<name>
GET    /api/admin?full=1
PATCH  /api/admin?id=<spinId>
DELETE /api/admin?id=<spinId>
DELETE /api/admin?id=<spinId>&rider=<name>


BEHAVIOUR NOTES
---------------
- ICE details are visible to:
    1. The spin proposer (via /api/ice, matched by author name)
    2. The admin (via /api/admin, matched by ADMIN_TOKEN_SECRET)
- ICE details are auto-deleted 24 hours after a spin's scheduled
  start time by BOTH a read-time purge and a nightly cron job.
- Committing without ICE is not possible; the server rejects it.
- The admin token should be kept secret. Change it in Netlify's
  Environment variables whenever needed — the admin page
  will accept whatever value you set.



  ------------------------------------------------------------
Version: Proposer Edit + Cancel
Date: 2026-09-25
------------------------------------------------------------

FEATURE: Proposer can now Edit and Cancel their own spins.

FILES MODIFIED
--------------
netlify/functions/spins.js
  - Added DELETE handler. Authorised by either:
      1. Matching user name (spin.author) sent as ?user=... in query
      2. Valid X-Admin-Token header (admins always allowed)
  - On successful delete, fires a push notification to subscribers
    titled "❌ Spin Cancelled" with the spin title, date, and time.
  - Added proposer-edit branch to PATCH. Triggered by sending
    { _edit: true, _requester: <name>, ...fields } to the same endpoint.
    Only the spin author (or admin) can use this path.
  - Editable fields for proposer: title, date, time, location,
    distance, pace, minRiders, weatherPolicy, phone, mapLink,
    mudguardsRequired. Committed riders and ICE are preserved.

public/app.js
  - renderSpinCard: if the current user is the proposer of the
    spin, a "Yours" badge appears and an "Organiser Tools" row
    is added with Edit and Cancel buttons.
  - openEditSpinModal / saveProposerEdit: new inline edit modal
    that only proposers see. Pre-fills all fields, sends PATCH
    with _edit: true.
  - openConfirmCancel / cancelSpin: new confirm modal, then
    DELETE to /api/spins?id=...&user=<name>.
  - injectProposerModals: both new modals are injected into the
    DOM at load time via JS. No changes to index.html needed.

public/sw.js
  - Cache version bumped braycc-v3 → braycc-v4 to force all
    browsers to fetch the new app.js on next visit.

PUBLIC BEHAVIOUR CHANGES
------------------------
- Spin cards proposed by you now show a purple "Yours" badge.
- A dotted border section labelled "Organiser Tools:" appears
  with two buttons: Edit and Cancel.
- Edit opens a full modal to change any field of your spin.
  Existing RSVPs, ICE contacts, and committed riders are kept.
- Cancel permanently deletes the spin (with confirmation).
  Subscribers are notified via push that it was cancelled.
- Other users do not see these buttons — they can only RSVP.

SECURITY NOTE
-------------
Authorisation is by name matching (as with the rest of the app),
plus the ADMIN_TOKEN_SECRET header for admin override. This is
appropriate for a small club. For stricter control, add real
authentication in a future update.

------------------------------------------------------------
Version: Floating WhatsApp contact button
Date: 2026-09-25
------------------------------------------------------------

FEATURE: Floating "Ask a question on WhatsApp" button.

FILES CREATED
-------------
public/whatsapp-numbers.json
  - Config file for the WhatsApp picker.
  - Contains: heading, intro, defaultMessage, contacts[].
  - Each contact has: label, name, phone (+international), description.
  - Edit this file to add/remove/change numbers — no code changes needed.

FILES MODIFIED
--------------
public/index.html
  - Added floating green WhatsApp button fixed bottom-right.
  - Added picker modal (bottom-sheet on mobile, dialog on desktop).
  - Both are pure markup, driven by app.js.

public/app.js
  - Added whatsAppConfig global.
  - Added loadWhatsAppNumbers() — fetches /whatsapp-numbers.json on startup.
    If the config is missing or has no contacts, the button stays hidden.
  - Added openWhatsAppPicker() — renders contact list with wa.me links,
    each pre-filled with the defaultMessage from config.
  - Added closeWhatsAppPicker() + backdrop click + Escape key handlers.

public/sw.js
  - Cache bumped braycc-v5 -> braycc-v6.

  ------------------------------------------------------------
Version: Phone collection on RSVP + WhatsApp group builder
Date: 2026-09-25
------------------------------------------------------------

FEATURE
-------
- Phone number is now collected whenever a user marks
  Interested OR Committed on a spin.
- Committed still also requires an ICE emergency contact.
- The spin proposer sees a "WhatsApp Group" button on their
  own spins, which opens a modal with:
    * All RSVP'd names + phone numbers
    * Status (Committed / Interested)
    * A one-click "copy all numbers" button
    * A pre-filled suggested group message
    * Step-by-step instructions for creating the group

FILES MODIFIED
--------------
netlify/functions/spins.js
  - PATCH now requires `phone` for both `interested` and
    `committed` actions.
  - committed entries gain `phone` field.
  - interested entries now stored as objects { name, phone }
    instead of plain strings.
  - sanitizeSpin strips phone numbers from public responses
    for both committed and interested riders.
  - New endpoint: GET /api/spins?waGroup=1&id=...&user=...
    Returns phone numbers + suggested message for the group,
    only to the spin author.

public/index.html
  - ICE modal rebuilt as a unified RSVP modal:
      * Phone field always shown (top)
      * ICE fields only shown when Committing
      * Title/subtitle/submit button text adapt to the action
  - New WhatsApp Group modal (proposer only).

public/app.js
  - toggleRSVP now opens the modal for both Interested and
    Committed (previously only for Committed). "Un-committing"
    still happens silently without the modal.
  - openICEModal adapts to rsvpType — hides ICE for interested,
    shows for committed.
  - submitRSVPWithDetails replaces submitICEAndCommit.
  - Phone is cached in localStorage under braycc_user_phone
    for convenience.
  - New: openWAGroupModal / closeWAGroupModal /
    copyAllPhoneNumbers / copySuggestedMessage.
  - renderSpinCard: added "WhatsApp Group" button to
    proposer controls.

public/sw.js
  - Cache bumped braycc-v7 → braycc-v8.